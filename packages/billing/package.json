{
  "name": "@omdala/billing",
  "version": "0.1.0",
  "description": "Pricing, promo, and checkout engine for OMDALA",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "npm run build",
    "test": "node --test dist/__tests__/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.19.42",
    "typescript": "^5.9.3"
  }
}
