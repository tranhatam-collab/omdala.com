import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("Om AI")
                .font(.largeTitle)
            Text("Trusted execution cockpit")
                .foregroundStyle(.secondary)
        }
        .padding()
    }
}
