# services/api

Cloudflare Worker skeleton for `api.omdala.com`.

## Endpoints

- `GET /api/health`
- `GET /api/session`
- `POST /api/auth/magic-link`

## Commands

From the repo root:

```bash
npm run dev:api
npm run build:api
```
